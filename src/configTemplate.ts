export let config = {
    "token": "",    // The bot key
    "prefix": "!",         // Command prefix,
    "commands": [           // The commands for the bot
        "kick",
        "ban",
        "purge",
        "serverinfo",
        "vote"
    ],
}
