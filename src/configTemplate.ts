export let config = {
    "token": "12345678",    // The bot key
    "prefix": "do",         // Command prefix,
    "commands": [           // The commands for the bot
        "kick",
        "ban",
        "purge"
    ]
}