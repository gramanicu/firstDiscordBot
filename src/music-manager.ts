import * as Discord from "discord.js"
const ytdl = require("ytdl-core");

export class MusicManager {
    public play(connection: Discord.VoiceConnection, song: string) {
    }

    public skip() {
    }

    public stop() {
    }
}